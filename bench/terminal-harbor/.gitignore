# Provider key file — never commit.
/.env
/.env.bak

# Python adapter bytecode.
__pycache__/
*.pyc

# uv-managed virtualenv (the lockfile IS committed for reproducibility).
/.venv/

# Harbor run output (trials, logs, results) + any local trace/results mirrors,
# regenerated each run.
/runs/
/results/
/trace/
/output/
/.harbor/
