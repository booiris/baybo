// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

/**
 * One individual run (one `results-*.json`) for the History lens and
 * the trend charts. `merged-*` consolidations are included with
 * `is_merged = true` so the UI can fold or flag them.
 */
export type RunSummary = { bench: string, 
/**
 * Stable per-bench key = the results filename without `.json`
 * (e.g. `results-agent-2026-06-13__15-40-24`, `merged-agent`). The
 * API addresses a run by this; `run_id` (in-JSON) is not unique
 * across arms.
 */
run_key: string, 
/**
 * The run id recorded inside the file (may repeat across arms).
 */
run_id: string, arm: string | null, model: string | null, dataset: string | null, started_at: string | null, ended_at: string | null, duration_ms: number | null, n_passed: number, n_total: number, pass_rate: number, mean_f1: number | null, total_cost_micro_usd: number | null, input_tokens: number | null, output_tokens: number | null, cached_input_tokens: number | null, mean_latency_ms: number | null, is_merged: boolean, };
