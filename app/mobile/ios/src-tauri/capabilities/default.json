{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the Aura mobile app (all platforms).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
