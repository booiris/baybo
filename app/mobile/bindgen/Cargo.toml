[package]
name = "baybo-mobile-bindgen"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "uniffi-bindgen CLI for the Baybo mobile core (kept out of the lib build)"
publish = false

[[bin]]
name = "uniffi-bindgen"
path = "src/main.rs"

[dependencies]
uniffi = { workspace = true, features = ["cli"] }
