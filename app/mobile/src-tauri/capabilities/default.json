{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the Baybo mobile app (all platforms).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
