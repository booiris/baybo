// Intentionally empty. The signing team (DEVELOPMENT_TEAM) is set directly in
// the target build settings — see `DEVELOPMENT_TEAM` in project.yml and the
// generated .xcodeproj. This file only exists so `configFiles:` in project.yml
// (and the .xcodeproj's base configuration reference) still resolves.
