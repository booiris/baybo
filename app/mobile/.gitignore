/target
web/dist/
# `tsc -b`'s incremental cache. Machine-local by construction — it records
# absolute paths and mtimes, so a committed one is stale for everyone else.
web/*.tsbuildinfo
web/node_modules/
