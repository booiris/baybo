{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the Aura main window — core APIs only; the app's own commands (get_connection, setup_*) are always permitted for its windows.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
