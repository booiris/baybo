[package]
name = "wire"
version.workspace = true
edition.workspace = true

[lib]
doctest = false

[features]
# Emits ts-rs-generated TypeScript definitions for the wire types
# (`Frame` / `Message` / …) into `sidecars/sdk/channel-ts/src/generated/`, the
# same surface `baybo-channels` used to own. Kept behind a feature so CI /
# release builds don't depend on ts-rs. Enables `baybo-model/ts-export` so
# the transitively-referenced `ApprovalDecision` / `ResourceAccess` emit too.
ts-export = ["dep:ts-rs", "baybo-model/ts-export"]

[dependencies]
baybo-model = { workspace = true }
chrono = { workspace = true }
serde = { workspace = true }
rmp-serde = { workspace = true }
thiserror = { workspace = true }

# Optional — only pulled in for the `ts-export` feature so that
# release builds don't have to compile ts-rs.
ts-rs = { workspace = true, optional = true }

[dev-dependencies]
