{
  "name": "@baybo/channel-telegram",
  "version": "0.1.0",
  "private": true,
  "description": "Telegram channel sidecar for the Baybo gateway",
  "type": "module",
  "bin": {
    "baybo-channel-telegram": "./dist/index.js"
  },
  "baybo": {
    "domain": "channel"
  },
  "scripts": {
    "build": "tsc",
    "bundle": "bun build src/index.ts --target=bun --minify --outfile=dist/bundle.mjs",
    "check": "tsc --noEmit",
    "start": "node dist/index.js",
    "test": "node --test 'test/**/*.test.mjs'"
  },
  "dependencies": {
    "@baybo/channel-sdk": "workspace:*",
    "grammy": "^1.30.0",
    "telegramify-markdown": "^1.3.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=21"
  }
}
